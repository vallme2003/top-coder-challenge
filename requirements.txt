# Core machine learning dependencies
scikit-learn>=1.0.0
numpy>=1.20.0
joblib>=1.0.0

# Optional dependencies for enhanced functionality
pandas>=1.3.0  # For data analysis (optional)

# Development and testing dependencies
coverage>=6.0  # For test coverage analysis (optional)
pytest>=6.0    # Alternative test runner (optional)

# Note: The core system only requires scikit-learn, numpy, and joblib
# Other dependencies are optional for development and analysis